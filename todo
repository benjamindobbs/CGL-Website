- When online payment handling is added, the paid order flow must also write to
  the financial ledger (transactions table) so /admin/transactions and the CSV
  export stay complete. One recordTransaction() call per line item, vendor
  "Online Pay", account = item category, source = "online_order". Refunds record
  a matching "withdrawal". See TODO(online-payments) in server/routes/orders.js.
